# Wyoming-OpenClaw configuration
# Copy this file to .env and fill in your values:
#   cp .env-template .env

# ── Required ─────────────────────────────────────────────────────────────────

# OpenClaw Gateway URL — use the gateway container's Docker DNS name.
# Find the container name with: docker ps | grep openclaw
GATEWAY_URL=http://openclaw-gateway:18789

# OpenClaw Gateway auth token
# Run: openclaw config get gateway.auth.token
GATEWAY_TOKEN=your_gateway_token_here

# Docker network that your OpenClaw gateway container is attached to.
# Find it with:
#   docker inspect <gateway-container> \
#     --format '{{range $k, $v := .NetworkSettings.Networks}}{{$k}}{{end}}'
OPENCLAW_NETWORK=openclaw_default

# ── Optional ─────────────────────────────────────────────────────────────────

# Session ID for persistent conversation context across voice commands
SESSION_ID=voice-assistant

# Home Assistant URL and long-lived access token for direct device control.
# Use the HA container's Docker DNS name if running in Docker, otherwise the hostname.
# Without these, all commands are forwarded to OpenClaw.
# Create a token at: Home Assistant → Profile → Long-Lived Access Tokens
HA_URL=http://homeassistant:8123
HA_TOKEN=your_ha_token_here

# Set to 1 to enable verbose debug logging
DEBUG=
